//to extracct the constants and store it in here
export const BASE_URL = ""; //why empty?? because already inside our vite.config, we are already setting our api to localhost 3000

export const USERS_URL = "/api/v1/users";
